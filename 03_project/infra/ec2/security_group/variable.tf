variable "web_port" {
    description = "The port will use for HTTP requests"
    type = number
    default = 80
}

variable "ssh_port" {
    description = "The port will use for HTTP requests"
    type = number
    default = 22
}

variable "https_port" {
    description = "The port will use for HTTP requests"
    type = number
    default = 443
}